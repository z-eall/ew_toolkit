# Ew Wiki Real Build — Map

Labels: wayfinder:map

## Destination

A real, hub-integrated wiki Tool — **`ew_wiki`** — living inside the `ew_toolkit` repo, built with Astro Starlight, deployed to GitHub Pages under the nested subpath `/ew_wiki/ewp/`. EWP is the first "component" inside the `ew_wiki` hub; the nested path leaves room for a future second mod's guides to slot in later without breaking bookmarks.

Reaching the destination means: v1 content is live on the real hub —
1. Structure + visual design settled (own ticket, before anything moves).
2. Built as its own Tool folder in `ew_toolkit`, reusing the proven prototype (including its working Monaco+monaco-yaml playground) as the starting point.
3. Preparation/"How to Start" page, Core Vocabulary page, a house-style ticket, and a reader-facing "How to use this guide" page all exist.
4. The 6 already-audited fundamental + basic-use-case guides are ported in as real content.
5. The site is registered on the landing page and deployed to GitHub Pages.

Each earlier ticket is built and verified **locally**; deploy is the last ticket, not a running assumption.

This map picks up where the [Wiki Tool Prototype map](../wiki-tool-prototype/map.md) left off — that map resolved framework (Astro Starlight) and go/no-go (Go), and named the conditions this map's v1 opens with.

## Notes

- Domain: Valheim modding / EWP-family tooling — see [CONTEXT.md](../../CONTEXT.md). `CONTEXT.md` covers the *toolkit project's own* vocabulary (Scripter, Schema, Tool, Subpath, etc.) — it explicitly does **not** cover EWP/WEC's in-game scripting vocabulary (`data`, `f=`, `par`, `poke`, `fallback`, save/load keys). The Core Vocabulary page (ticket 04) is that second, EWP-specific dictionary — a different audience/purpose than `CONTEXT.md`, not a duplicate of it.
- Skills every session should consult: `ew_toolkit:grilling` and `ew_toolkit:domain-modeling` for any ticket marked `grilling`; `ew_toolkit:prototype` for ticket 01; `ew_toolkit:research` only with explicit signoff before tapping a new external source (standing user rule); `ew_toolkit:writing-for-agents` self-invoked before any structural `SKILL.md`/`AGENTS.md`/`CLAUDE.md` edit, with challenge-and-signoff first, even for a small diff.
- **EWP is primary, WEC is secondary/reference** (per `CONTEXT.md`) — WEC content stays folded into EWP's guides for v1, not its own component. Split it out later only if it earns enough content of its own (same "start compact, split when it outgrows itself" pattern already used for Core Vocabulary itself).
- **Expand World Data is not a locked target.** Building on EWP + WEC first; if a real guide topic turns out to need Expand World Data to make sense, revisit then — not pre-scoped in or out.
- **Why nested from day one** (`/ew_wiki/ewp/...`, not flat `/ew_wiki/...`): Starlight URLs are generated straight from folder structure, so nesting now costs nothing. Starlight does have a built-in `redirects` config for a later flat→nested migration, but that redirect list has to be hand-written at migration time — skip it and every existing bookmark 404s. Nesting now removes that risk entirely given a second mod is a real possibility, not a hypothetical.
- **v1 content = 6 of the 9 existing, already content-reviewed guide files** — the fundamental + basic tier, not all 9:
  - Theory Fundamental (2): `EWP_Understanding_Fields.md`, `EWP_How_To_Use_Data.md`
  - Basic Use Cases (4): `EWP_Basic_Poke_Guide.md`, `EWP_Guide_to_PARS.md`, `EWP_Type_Time_Guide.md`, `EWP_Type_Realtime_Guide.md`
  - Advanced Use Cases (3, **deferred to phase 2**): `EWP_Advanced_Poke_Guide.md`, `EWP_Maths_Guide.md`, `EWP_Ship_Modification_Guide.md`
  - Source folder: `ew_wiki/docs/guide-source/` (moved into the repo during ticket 04's rework; was `C:\Users\Ultimate\Claude\ew_toolkit_wiki_prototype\guide-reference\`, subfolders originally renamed `1-theory-fundamental`/`2-basic-use-cases`/`3-advanced-use-cases`, filenames unchanged; relocated under `docs/` in round 3 to stop colliding with `docs/sources.md`'s folder name; subfolders renamed again to `1-beginner-guide`/`2-intermediate-guide`/`3-advanced-guide` per maintainer request). Maintainer intends to add more script examples directly into this folder before ticket 07.
- **v1 IA is deliberately thin**: Start Here/Prep, Concepts (`fields.mdx`, `data.mdx`, `script.mdx` — the shared Vocabulary page and `schema`/`scripter` pages were tried and scrapped in ticket 04's round 2, see its Answer), and the ported guides once they land. No standalone Reference/Recipes/Troubleshooting sections in v1 — their early placeholder stubs were removed as more confusing than useful; a section comes back only once it has real content — see Not yet specified.
- Reuse-before-building and $0-forever apply per `AGENTS.md` — reuse the prototype's Starlight setup and Monaco playground rather than rebuilding, pending ticket 01's structure/design comparison.
- `ew_toolkit`'s own `package-lock.json` must be generated on Linux, not Windows, once real `npm install` happens inside the actual repo (ticket 02 onward) — not relevant to the standalone prototype folder.
- Prior map's plain-language primer (Static site generator / Astro Starlight / Monaco / monaco-yaml) still applies — see [wiki-tool-prototype/map.md](../wiki-tool-prototype/map.md#notes).
- **Content-authoring standing rules now live in [`ew_wiki/AGENTS.md`](../../ew_wiki/AGENTS.md)**, not duplicated here — voice/depth (warm tone, simple-language-but-full-step-by-step-depth for beginners), dedicated-server-as-default scope for every guide, mod-install-out-of-scope, source-verify (pointing at [`ew_wiki/docs/sources.md`](../../ew_wiki/docs/sources.md)), and cross-link-on-first-use. Surfaced across the Preparation ticket's reopen and rescope (see its Answer, and ticket 04's Question) — applies to every remaining content ticket (04, 06, 07), not just those.
- **Interactive-widget phase (tickets 17-22).** Six candidate widgets, same spirit as the existing `FilterLimitExplainer.astro` on `advanced-filter-plural.mdx` (paste a real script block, poke at it, get a live pass/fail with a plain-English reason) — reviewed and ordered by the maintainer, chained 17→18→19→20→21→22 via `Blocked by` so the frontier only ever surfaces one at a time. **"Cleanly build one live before moving to the next" is a hard sequencing constraint from the maintainer, not a suggestion**: finish and build-verify each (real component landed, wired into its page, `build-check.sh` clean) before the next ticket unblocks. Each ticket goes through `ew_toolkit:prototype` first when the design isn't already obvious, then the real Astro component. A 7th candidate — `objects:`'s own `weight:` vs. a rule's top-level `weight:`, "same word, different job" — folds into ticket 17, not a separate ticket.

## Decisions so far

- [Structure and visual design comparison](issues/01-structure-and-design.md) — Structure: adopt nesting everything under one `ewp/` group with a thin hub landing page and `/ew_wiki/ewp/...` routes, as-is. Visual: adopt Variant A (Baseline) — the shared 5-color hub palette mapped onto Starlight's own bg/panel/text/hairline variables, Starlight's default accent color left untouched. 3 other palette variants (Monochrome, Layered, Warm accent) built and rejected — full comparison in the ticket's Answer.
- [Move into the hub](issues/02-move-into-hub.md) — Real `ew_toolkit/ew_wiki/` Tool folder created from the prototype, ticket 01's reshape applied, base `/ew_wiki/` (routes land at `/ew_wiki/ewp/...`), `src/styles/theme.css` imports `shared/theme.css` (Variant A mapping). `npm install`/build done via WSL, never Windows; `package-lock.json` copied back. Build verified clean (8 pages). Landing-page registration and CI wiring deferred to ticket 08.
- [Preparation / How to Start page](issues/03-preparation-page.md) — Published once with wrong facts (unconfirmed assertions), reopened by the maintainer, redone as a grilling ticket. `ewp/preparation.mdx`: SP gets one callout only (dedicated server is now this wiki's default assumption everywhere, not just this page — see Notes), Dedi setup covers both self-hosted and rented/FTP hosting, mod installation dropped as out-of-scope, script "install" = save the `.yaml` into the server's config folder, and a dedicated section walks the client-capture-then-server-paste `data.yaml` workflow (WEC writes client-side, EWP reads a separate server-side copy). Also produced the new `ew_wiki/AGENTS.md` + `ew_wiki/docs/sources.md` and a "Confirm, don't guess" rule in root `AGENTS.md` — see Notes. Build verified clean (9 pages).
- [Core Vocabulary page](issues/04-core-vocabulary-page.md) — Round 1 built three pages (`data`/`script`/`vocabulary`) and fixed a pre-existing `.md`-vs-`.mdx` rendering bug on `schema.md`/`scripter.md` (tickets 01/02) — see the ticket's Answer for that bug and the content-layer cache trap that masked it during verification. Reopened by the maintainer as too advanced for a beginner audience. **Round 2**: scrapped `schema`/`scripter`/`vocabulary` and the placeholder Reference/Recipes/Troubleshooting sections entirely; rewrote `data.mdx`/`script.mdx` in simpler language with beginner-relevant example fields and no early `par`/`poke` name-drops; added a new `fields.mdx` from `EWP_Understanding_Fields.md`; set explicit sidebar order (Fields → Data → Script); added a wiki-wide YAML indent rule to `ew_wiki/AGENTS.md` (nested list items align with their parent key); moved the guide source folder into the repo (`ew_wiki/guide-source/`, see Notes). Build verified clean via WSL, 7 pages. The collapsible/scroll-following TOC sidebar the maintainer also asked for was split out to [ticket 09](issues/09-collapsible-toc-sidebar.md) rather than bundled in. **Round 3**: source-verified every fact in `fields.mdx` against real mod source (caught `activationDistance`→`activationRange`, confirmed `type: create` over the previously-shipped `type: spawn`); hardened `ew_wiki/AGENTS.md`'s source-verify rule into an ordered 4-step checklist plus a "Never guess" section; added per-page `complexity` frontmatter badges via a `PageTitle.astro` override; rebuilt the Home page as a real hub page (doc layout, "Overview"/"Choose a Guide" heading, no TOC panel, per-mod tagline sourced from that mod's own GitHub description); renamed the site to "Expand World Wiki"; moved `guide-source/` under `docs/` to stop it colliding with `docs/sources.md`'s own folder name.

- [House-style](issues/05-house-style.md) — Distilled the three content-review reports into a "Component conventions" section in `ew_wiki/AGENTS.md`: 7 rules covering `<Steps>`/`<Aside>`/`<Tabs>` (when to use each and when not to), shared snippets for repeated boilerplate, code-fence language tags, splitting pages by confidence/purpose, and a playground-suitability rule (schema-valid ≠ behavior-correct). Also ran a project-wide AGENTS.md/CLAUDE.md dedupe pass: cut a byte-identical, dead-linked duplicate of the root project's Agent-skills pointer from `ew_toolkit/AGENTS.md` (mirrored into `ew_toolkit-cursor/AGENTS.md`), and merged `ew_wiki/AGENTS.md`'s "Never guess" restatement into its adjacent source-verify checklist.
- **Beginner Concepts/Examples expansion** (ahead of ticket 07, ad hoc per maintainer request) — proposed a tiered topic list (Beginner/Intermediate/Advanced) across Concepts and a new Examples section; maintainer chose to extend existing pages over adding new overlapping ones. Extended `script.mdx` with `type:`'s trigger list + parameter (caught and fixed a factual error here — the comma after a type is its parameter, not a second type; confirmed against EWP's `docs/scripting.md`), `filter:`/`bannedFilter:`, and `spawn:` as a second rule action; extended `data.mdx`'s value-kind list per a maintainer-linked WEC source (deeper loot/randomization content deliberately deferred to a future intermediate topic). Added the first real Examples-section page, `ewp/examples/first-script.mdx` (walks the maintainer's own `simple.yaml` step by step) — this is also the reason "Recipes" (early placeholder name) is now "Examples" in `astro.config.mjs`. Also folded a "back to top" button request into [ticket 09](issues/09-collapsible-toc-sidebar.md) rather than building it standalone. Build verified clean via WSL, 7 pages.

- [Advanced Functions rework](issues/10-advanced-functions-rework.md) — reordered Time → Custom data (global) → Vector → Long numbers; Time gained a new case study (clamped event countdown nesting `<day>` inside `<sub>`/`<max>`); Vector and Long numbers shrunk to a quick-reference note at the bottom. Custom data (global) left untouched — ticket 11 owns extracting it into its own page. Build verified clean, 30 pages.

- [EWP Key page](issues/11-ewp-key-page.md) — new page (renamed from "Custom Data: Advanced EWP Key" to "Custom Data: EWP Key", slug `ewp/concepts/ewp-key/`, after chartering), successor to "Custom Data: Your Own Flags," covering `exec:`/`<save_X_Y>`/`<load_X>`/`<clear_X>`, an EWP-key-vs-`globalkey` comparison, a short `type: key` section, and simple/advanced worked examples. Pulled the Custom data (global) section out of `advanced-functions.mdx` (a short pointer now stands in its place) and split `advanced-triggers-no-prefab.mdx`'s combined `key`/`globalkey` heading into two. Build verified clean, 31 pages.

- [Advanced Functions reframed as a creative-combo showcase](issues/12-advanced-functions-reframe.md) — Time functions split by clock (game-clock to `advanced-triggers-time.mdx`, real-world-clock deleted as a duplicate of `advanced-triggers-realtime.mdx`); Advanced Functions' remaining job is showing Numeric/Text functions combined creatively, not teaching a new category. Registered `EWP_Maths_Guide.md` in `docs/sources.md`. Found and fixed a stale cross-page anchor plus a silent heading-slug gotcha (`"Case study - X"` → triple-hyphen id, `"Case study: X"` → single-hyphen). Build verified clean, 31 pages.

- [Advanced Filter rework + site-wide URL rename & Advanced reorder sweep](issues/13-advanced-filter-and-url-rename-sweep.md) — Split `advanced-filtering.mdx` into `advanced-filter-plural.mdx` ("Advanced Filter: Plurals", reworked procedurally: standalone `filters:` first, then nested inside `objects:`/`poke:`) and a new `advanced-filter-condition.mdx` ("Advanced Filter: Condition", full simple-then-advanced treatment). Site-wide URL rename sweep (11 files: `fields`→`basic-fields`, `data`→`basic-data`, `script`→`start-scripting`, `triggers`→`basic-triggers`, `filter`→`basic-filter`, `adding-randomness`→`basic-rng`, `poke`→`basic-poke`, `pars`→`basic-pars`, `functions`→`basic-functions`, `rpcs`→`basic-rpcs`, `advanced-filtering`→`advanced-filter-plural`), plus a sidebar-label-only rename (`objects-filtering.mdx` → "Filter: by Objects"). Advanced sidebar reordered to echo Beginner/Intermediate order, `advanced-filter-condition` forced last. Build verified clean, 32 pages.

- [Extended Reading rework](issues/14-extended-reading-rework.md) — Extended Reading reordered/renamed to Bee Ecosystem Overhaul → Build Custom Ship with Data (`custom-ship-data`, was `ship-modification`) → World Boss Progression (`world-progression`, was `world-bosses`) → Auto-upgrade Stations (new) → Village Cargo Mission (new). `village-economy.mdx` deleted, split into `auto-upgrade-station.mdx`/`village-cargo.mdx` per real source confirmation (two always-separate file pairs in `docs/guide-source/4-script-examples/3-ewp-advanced/`). Added the station tier table, the village/cargo name lists, and the cargomaster NPC pattern that the old page had cut as repetitive. The three progression pages (World Boss Progression / Auto-upgrade Stations / Village Cargo Mission) now cross-link as one source-confirmed system sharing the `bfvworldlevel` key, standalone-first. Build verified clean, 33 pages.

- [Doc-example schema hook, injectData/triggerRules corrections](issues/15-doc-example-schema-hook.md) — Source-verified `injectData:` and `triggerRules:` directly against EWP's C# (`PrefabManager.cs`/`PrefabData.cs`), since `scripting.md`'s prose doesn't mention `data:` needing `triggerRules: true` to fire `type: change`. Added that section to `advanced-triggers-change.mdx`, a beginner-tone `injectData:` blurb to `basic-data.mdx`, and registered both C# files in `docs/sources.md`. Also added a new hook, `guard-doc-example-schema.cjs`, that runs every doc `\`\`\`yaml` example through `ewp_validator`'s real structural checker — caught a real mistake (a `type: change` example missing `prefab:`) live during this same ticket.
- [Scrap media placeholders](issues/16-scrap-media-placeholders.md) — Dropped the "leave a numbered `<Aside>` placeholder for the maintainer to capture later" mechanism entirely: removed the rule from `ew_wiki/AGENTS.md` and the 3 placeholders it had produced (`basic-poke.mdx`, `advanced-rpcs.mdx`, `custom-ship-data.mdx`).
- [Who's nearby widget](issues/17-widget-objects-filtering.md) — Built `ObjectsNearbyExplainer.astro`, wired into `objects-filtering.mdx`; 8 prototype rounds before go-ahead, several more live-page rounds closing gaps the prototype missed (stray global CSS margin, hardcoded box heights, absolute-position glyph hacks, spacing). Build verified clean, 33 pages.
- [Expression checker widget](issues/18-widget-advanced-filter-condition.md) — Built `ConditionExplainer.astro`, wired into `advanced-filter-condition.mdx`; 4 prototype rounds. Checking EWP's own `ConditionsTests.cs` surfaced two real content fixes to the page itself: operator precedence is actually documented (`not > and > xor > or`, was previously flagged as unconfirmed) and `in`/`not in` does substring matching, not strict list membership. Follow-up round fixed 4 internal-bookkeeping leaks into reader text (added `guard-reader-internal-bookkeeping.cjs` to prevent recurrence) and 2 widget spacing bugs; `ew_toolkit-cursor` worktree found multiple sessions out of sync and caught up in the same pass. Build verified clean, 33 pages.
- [Trigger-change "would this fire?" widget](issues/19-widget-triggers-change.md) — Built `TriggerChangeExplainer.astro`, wired into `advanced-triggers-change.mdx`; 1 prototype round. Reuses `ConditionExplainer.astro`'s construction pattern with tighter spacing for its smaller content. Widget shows two independent gates (the `type: change` value condition, the `triggerRules:` gate) both needing to pass to fire — teaching the "look away" trap concretely. Also caught a 5th internal-bookkeeping leak on the same page (see ticket 18). Build verified clean, 33 pages.
- [Nesting-peel widget](issues/20-widget-advanced-functions-nesting.md) — Built `NestingPeelExplainer.astro`, wired into `advanced-functions.mdx`; 1 prototype round. Peels `<max_0_<sub_20_<len_X>>>` one layer at a time via a Back/Next/Reset step control (reusing ticket 17's story-walkthrough pattern), showing the reader's real chosen name (shortened to "first ... last" only past 20 chars) until each layer resolves into a chip. Maintainer feedback swapped the prototype's free-text name input for a fixed 3-scenario dropdown covering all three clamp shapes: plain, exactly-at-limit, and negative-clamped-to-0. Three follow-up rounds fixed, in order: a redundant name-preview line + hard-height clipping on mobile; Back sitting 8px out of line with Next/Reset from Starlight's own recurring stray `margin-top:16px` bug (same as tickets 17-18), plus dropped arrow glyphs/step-of-3 label as noise; and a real regression where the dropdown swap had silently reverted the unresolved `<len_X>` display back to a generic placeholder instead of the real name. Closed — no further known gaps. Build verified clean, 33 pages.
- [Lottery widget](issues/21-widget-basic-rng-lottery.md) — Built `WeightLotteryExplainer.astro`, wired into `basic-rng.mdx` after the weight-pool gap example (step 5); 1 prototype round, then a round-2 rework adding a Weight-vs-Chance tab switch. Both mechanics source-verified against `ExpandWorldPrefabs` before encoding: `InfoSelector.cs`'s `Randomize()` confirmed the proportional weight-pool model (total floors at 1, under-1 pools leave a real gap); `InfoManager.cs`'s `Add()` then showed `chance:`-only rules land in a separate "Separate" bucket where every matching rule fires independently — under `chance:` alone two same-trigger rules can both/one/neither fire, unlike `weight:` where at most one ever can. Widget now switches both the shown script field and the underlying simulation per tab, not just a relabeled number. Two real bugs caught and fixed: JS-created tally/script elements needing `:global(...)` CSS (missed once, same class of bug `FilterLimitExplainer.astro` already warns about), and a mobile layout break from leftover cross-card `grid-column` placement, fixed by restructuring to two self-contained cards instead of four separately-placed grid cells. Closed — no further known gaps. Build verified clean, 33 pages.
- [World-level dial widget](issues/22-widget-world-level-dial.md) — Built `WorldLevelDialExplainer.astro`, wired into all 3 real pages sharing `bfvworldlevel` (`world-progression.mdx`, `auto-upgrade-station.mdx`, `village-cargo.mdx`), each opening the widget on its own matching tab. The ticket's own cross-page-sync fork (independent state per page vs. a real persistence mechanism) got a third option from the maintainer instead: one widget, 3 tabs, sharing a single 0–8 slider — no navigation or persistence edge cases needed to see all 3 systems react together. 1 prototype round, then a maintainer round added a "fallback" pill (distinct amber accent) for the boss-progression overflow tier (level 8) and the auto-upgrade tables' own max-tier levels (7–8). A round-2 fix made every result table's columns fill the box edge to edge and line up with the 3 tab buttons above (`table-layout: fixed` + explicit width classes, 1/3+2/3 split for Stations' 2-column table), and dropped the Stations tab's feast-table section as not important for the demo. Round 3 found the round-2 fix hadn't actually reached the Cargo tab — root cause was Starlight's own doc CSS forcing every `<table>` to `display: block`, which silently defeats `table-layout: fixed` (masked by accident on Boss Progression's longer text, visible on Cargo's short numbers); fixed by forcing `display: table` back on. Round 3 also made the whole widget static — measured (not guessed) the tallest real render across every tab × slider level at both widths and reserved that space via `min-height`, so switching tabs or dragging the slider never resizes the box. Applied the `:global(...)` JS-created-element CSS guard from the start this time instead of catching it after the fact. This closes the interactive-widget phase (tickets 17–22) — no further widget ticket remains on this map. Build verified clean, 33 pages.

## Not yet specified

- **Phase 2 content**: the 3 Advanced Use Cases guides, plus growing the "ultimate guide" beyond the original 9 source files as an open-ended, beginner→intermediate→advanced EWP+WEC reference. Not sized or ticketed yet — revisit once v1 ships and the pattern is proven.
- **WEC as its own component** — only if WEC content folded into EWP guides (per Notes) grows enough to justify splitting out. No trigger condition defined yet.
- **Other Jere mods as future `ew_wiki` components** — "we might be creating other mod of Jere within this wiki hub if the first one lands right" (user, charting session). Entirely contingent on EWP's component succeeding; no shape yet.
- **Reference section** (exhaustive, schema-generated "every key" pages, Factorio-prototype-docs style) — needs tooling to generate from the validator's schema that doesn't exist yet.
- **Troubleshooting section** — needs the validator's own diagnosis-category vocabulary/wording, a cross-Tool dependency not yet wired.
- **Checklists/quizzes** at section ends — mentioned in the original research report, not part of any locked v1 condition; revisit once v1 content exists to attach them to.
- **Expand World Data** — see Notes; only becomes a ticket if a real guide topic forces the question.
- **Visual media (screenshots/diagrams/video)** — never scoped anywhere on this map; surfaced by the maintainer during the Preparation ticket's correction pass. The "leave a numbered `<Aside>` placeholder for the maintainer to capture later" mechanism was tried (rule in `ew_wiki/AGENTS.md`, 3 placeholders landed across the wiki) and scrapped — see [ticket 16](issues/16-scrap-media-placeholders.md). Static screenshots/diagrams could still return later on a different mechanism if a specific spot genuinely needs one; embedded video stays out of scope for the same reason as before (no guide content needs motion/timing to explain).

## Out of scope

*(none yet — nothing has been ruled out past this map's destination so far)*
