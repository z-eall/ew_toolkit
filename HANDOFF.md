# Handoff

Last updated: 2026-09-16 (later). Continued the same [Player Identity & Object Ownership map](.scratch/ew-wiki-player-identity/map.md). **Ticket 13 (Effects → "Personal FX" + new "Projectile" entry) is closed and signed off. Ticket 11 (chunk 5) is mid-restructure, NOT signed off, NOT shown to the maintainer in the dev server yet — the page currently has real content changes (2 sections deleted, 1 merged) that need a live-render pass before anything gets closed.** See "This session (later)" immediately below before "This session (2026-09-16)" further down, which is now prior-session history.

## This session (2026-09-16, later): ticket 13 closed (Personal FX + Projectile); chunk 5 restructured, NOT yet shown/signed off

1. **Closed [ticket 13](.scratch/ew-wiki-player-identity/issues/13-fx-object-rule.md)** ("Effects (FX)" object-rule entry) — signed off after several rounds. Final shape: renamed "Effects" to **"Personal FX"** (weapon's own hit spark = attacker-owned; a `<Aside type="caution">` right after it for DoT FX/repeating effects like `vfx_Burning`, the one exception; then the wider "anything on your own body" rule covering both self-caused (`sfx_jump`, `fx_Eat`) and done-to-you (`vfx_player_hit`, `vfx_perfectblock`, `fx_crit`) effects). Added a brand-new **"Projectile"** entry (player-fired = same attacker-owned mechanism, confirmed via `Attack.cs`, no RPC; a Tip listing its own FX/spawn examples plainly, no script box; a Caution that a monster's projectile instead follows Zone Host, one real example `Greydwarf_throw_projectile`). Feast section absorbed the old broadcast-effect explanation standalone (no longer points back to FX). Recorded in the ticket's own Answer and the map's Decisions-so-far.
2. **Picked up [ticket 11](.scratch/ew-wiki-player-identity/issues/11-object-chunk-5.md)** (chunk 5) next, now unblocked. First did a standard review pass (added the missing "See every prefab..." sub-title links to Tameable/Character-Ragdoll/OfferingBowl, fixed the known-wrong OfferingBowl prefab from `BossStone_Fader` to the real `OfferingBowl`-carrying prefab `offeraltar_FrozenKing_bossroom` — confirmed `BossStone_Fader` actually carries a different, decorative-only component called `BossStone`). **Then the maintainer redirected the scope entirely**, asked in the same session:
   - **Verified two of the maintainer's own domain-knowledge claims against the decompile, both confirmed true:**
     - Riding a tameable creature really does write real ZDO fields — confirmed at `Sadle.cs:348` (`ZDOVars.s_user`, the rider's ID) and `Sadle.cs:491` (`ZDOVars.s_stamina`), plus an explicit `SetOwner(sender)` call at `Sadle.cs:351` that transfers actual ZDO ownership to the rider, not just those two named fields.
     - **New finding, not previously on the page**: taming a creature (or the wild→tamed transition itself) does **not** transfer ownership to whoever fed it — checked `Tameable.cs`'s `TamingUpdate`/`Tame()` methods line by line, both only run if you already own the creature (gated on `IsOwner()`), neither ever calls the game's ownership-claim function. This matches the project's own code-proof ledger (`EWP_pid_ownership_code_findings.md`), which had already logged the same finding independently.
   - **Per the maintainer's explicit instruction, removed the "Character (creatures) and their death Ragdoll" and "OfferingBowl (boss spawn)" sections from the live page entirely** (not deferred — deleted, script boxes and all). **Removed the standalone "Tameable" section too**, folding it into the existing **Sadle** entry instead as a one-line alias Tip: *"you can also make this work calling `Tameable`... only riding claims ownership this way; commanding/following never does."* Updated the top-of-page component table to match (20 entries now, was 23).
3. **All 24 YAML script fences re-validated clean** (`yaml-fences.cjs` + `yaml.parse`, 0 bad) after the deletions/merge. **Dev server restart was still in flight when this session ended — the maintainer has NOT yet seen this restructured page rendered live.** This is the single most important next step; do not close or further modify ticket 11 until that happens.
4. **Ticket 11's own file and the map are now stale relative to the live page** — ticket 11 still describes itself as "Tameable / Character-Ragdoll / OfferingBowl," but 2 of those 3 are now deleted from the page and the 3rd is merged into Sadle, not its own entry. Needs a rewrite (or a close-as-restructured + a fresh smaller ticket) once the maintainer signs off on the new shape — don't just paste the old question text into the Answer.

## This session (2026-09-16): ticket 10 closed; ticket 11 opened, mid-review with real unresolved corrections

1. **Closed [ticket 10](.scratch/ew-wiki-player-identity/issues/10-object-chunk-4.md)** (Fireplace/Fish/Gibber/TerrainModifier) — signed off after removing a "watch"-language clause from Gibber, an over-explaining clause from TerrainModifier, and the whole `allTerraformingFX` sub-example (maintainer judged out of scope). Verified a second, non-digging way the game spawns a TerrainModifier object (`Attack.SpawnOnHitTerrain`, `Attack.cs:1672-1703`) and **actually added it to the ledger this session** (`EWP_pid_ownership_code_findings.md`) — a prior claim that this was "already logged" turned out false and got fixed.
2. **Claimed [ticket 11](.scratch/ew-wiki-player-identity/issues/11-object-chunk-5.md)** (Effects/Tameable/Character-Ragdoll/OfferingBowl). Fixed the missing "*See every prefab with this component on Components/X*" sub-title links for Tameable, Character/Ragdoll, and OfferingBowl (these 3 + Effects had shipped without them). Renamed "Hit FX vs. damage-over-time FX" content is still mid-revision (see below, not finished).
3. **Real, confirmed mistake made and caught this session**: swapped the OfferingBowl script's placeholder prefab for `BossStone_Fader` — **this is wrong**, later confirmed (checking `valheimtools.stream`'s real prefab data) that `BossStone_Fader` carries the `BossStone` component, not `OfferingBowl`. Real `OfferingBowl`-carrying prefabs found: `Bonemass`, `Eikthyrnir`, `GoblinKing`, `Dragonqueen`, `FaderLocation`, others. **`Eikthyrnir` is the recommended real fix — not yet applied to the live page.** This is the single most important unfinished item.
4. **Long back-and-forth on the "Effects" entry, several real corrections found via decompile + EWP source (not the website, per explicit maintainer instruction) — none yet applied to the page:**
   - No single Component exists in code for `vfx_`/`sfx_`/`fx_` prefabs — confirmed via a whole-tree grep for those literal strings (zero hits anywhere in the decompiled game code). `ZSFX` is real but only for `sfx_`, and it doesn't affect actual `<pid>`/ownership at all (it reads an unrelated custom field used only for controller-vibration exclusivity). No decision yet on how to word this on the page, or whether to link anything at all.
   - **Fireplace section is confirmed inaccurate as currently live**: `type: change, fuel` does not reliably show "whoever interacted first" — toggling never touches `fuel` at all, and once a fireplace already has an owner, refueling reports the *existing* owner (often zone host), not the interactor. Confirmed directly against `Fireplace.cs:Interact()`/`RPC_AddFuel`. A fix was proposed in chat (switch to the on-demand `poke` pattern, like Smelter/Tameable) but **not yet applied**.
   - Found and verified (via `Attack.cs`, `Humanoid.cs`, `Projectile.cs`, this repo's own real scripts) a real family of "whoever personally does it, owns the effect, regardless of Zone Host" mechanisms distinct from Hit FX's original (wrong) citation — weapon hit spark (`vfx_player_hit`, `Attack.cs`'s own `m_hitEffect`, not `Character.cs`'s `m_hitEffects` as first claimed), perfect block (`vfx_blocked`), piece placement (`vfx_Place_*` family), and Fireplace/OfferingBowl's firework-item branch specifically (confirmed valid EWP wildcard syntax: `prefab: vfx_Firework_*`, case-insensitive prefix match, straight from EWP's own `PrefabHelper.cs`). None of this has been written into the ledger or the page yet — still at the "confirmed via research, discussing with maintainer" stage.
5. All 24 YAML fences still validate clean as of the last full page pass (before this session's still-open Effects/OfferingBowl/Fireplace corrections, which haven't touched a script block yet).

**Not yet done — ticket 11 is NOT ready to close:** the wrong OfferingBowl prefab needs fixing, the Effects entry's title/wording/component-link question needs a decision, the Fireplace section's inaccurate script needs fixing, and the newly-verified "I do it, I own it" prefab list needs to either get folded into the Effects entry or explicitly deferred. None of the last session's chat-only corrections have been written back into `object-ownership.mdx` or the ledger yet.

## Prior session (2026-09-15, later): ticket 7 closed, ticket 8 (ItemDrop/Sign/Sadle/Vagon) in progress

Picked up right after ticket 6 (explanatory-sections rewrite) closed — see the section below this one for that.

1. **Closed [ticket 7](.scratch/ew-wiki-player-identity/issues/07-object-chunk-1.md)** (Container/Trap/ItemStand/ArmorStand) — signed off after fixing 2 real field-name bugs (ItemStand/ArmorStand ZDO field names), 1 factual error (Trap's real ownership-transfer state), and reversing a wrong call I'd made about `prefab: Container`/`prefab: Trap` component-name targeting (initially rejected as invented syntax because I'd only checked this wiki's own docs; the maintainer pushed back and had me check EWP's own GitHub source instead, which confirms it directly — see the ledger's "value-group addendum"). Also caught and fixed a leftover lowercase `prefab: itemstand` typo after signoff review. Locked a house-style checklist this ticket (name "ownership" explicitly, never "claims it"/"watching", full-sentence italic sub-title links to valheimtools.stream, standardized check-command text, bare component-name headings) — **applies to every remaining chunk ticket (8-12) and the FX ticket (13).**
2. **Claimed [ticket 8](.scratch/ew-wiki-player-identity/issues/08-object-chunk-2.md)** (ItemDrop/Sign/Sadle/Vagon), applied the same house style, re-verified all 4 objects' real fields/prefabs against the ledger and decompile (all matched — no new bugs found in Sign/Sadle/Vagon).
3. **A real back-and-forth on ItemDrop's `pickedUp` mechanism** — the maintainer proposed using `filter: int, pickedUp, 1/0` at `type: create` to tell a tossed-from-inventory item apart from raw never-touched loot. I initially pushed back twice, having only checked the base game's own decompile: there, `pickedUp` is packed inside a binary `itemData` blob field, and the only place a bare `pickedUp` ZDO field exists is a one-time old-save-format migration in `ZDOMan.cs` that deletes it right after reading it — so I concluded it wasn't live syntax. **That was wrong** — checking EWP's own `docs/scripting.md` (not just the base game) shows EWP has its own auto-unpack layer that re-exposes `pickedUp` (among others: `durability`, `stack`, `quality`, `variant`, `crafterID`, `crafterName`, `worldLevel`) from that packed blob as an ordinary scriptable field. The maintainer's original example was correct; I hadn't checked the right source. Full finding, both wrong turns and the correction, logged in the ledger's new "`pickedUp`/EWP item-data-unpack addendum." **Lesson recorded there for tickets 9-12**: "not found in the base game's decompile" only rules out the base game — always check EWP's own docs/source for a compatibility layer before concluding a field doesn't exist.
4. Rewrote the ItemDrop section using the maintainer's own two examples (tossed vs. raw-loot, `pickedUp: 1`/`0`), with a plain-language definition of what "ItemDrop" means (the maintainer asked for less internal detail here than my first draft had).
5. All 25 YAML script blocks (the page now has one more than before, from the two new ItemDrop scripts replacing the one old poke-check) re-validated clean via `ewp_validator`'s CLI after every round. Dev server restarted (not reloaded, per the known WSL staleness bug) after each edit; also had one mid-session case where the maintainer couldn't open the preview at all — server was restarted fresh and that resolved it, root cause not otherwise diagnosed.

**Not yet done**: Sign/Sadle/Vagon sections in ticket 8 were verified against the ledger but not yet given a final live-render pass in front of the maintainer since the ItemDrop rewrite — that's the very next step, see below. Ticket 8 has not been signed off or closed yet.

## Earlier this session (2026-09-15): ledger hardened, verification hook built, page rebuilt into Prototype 4, PDF review loop set up

Picked up right where 2026-09-14's session left off (ledger/hook were only *planned*, nothing written to disk yet — see that session's section below).

1. **Harsh-critic self-review of spec.md + the ledger** (`.scratch/ew-wiki-player-identity/spec-pid-ownership-mechanic.md`, `ew_wiki/docs/guide-source/3-advanced-guide/EWP_pid_ownership_code_findings.md`), done inline per the maintainer's explicit "yourself, don't delegate" — found and fixed: the mandatory pre-check only grepped `ClaimOwnership(`/`SetOwner(`, missing `SetOwnerInternal(` as a third required grep (confirmed via decompile search that no existing ledger row was actually wrong because of the gap, but the check itself was incomplete); the ledger's Summary miscounted components (33→34, missing "Player (self-claim)").
2. **Prefab-citation audit, 3 rounds** — the maintainer caught 3 wrong prefab examples directly (`piece_spike_wood` isn't real → `fuling_trap`/`piece_trap_troll`; `piece_hearth` misspelled → `hearth`; the Sadle row named the rider's equip item instead of an actual prefab carrying the component → `Lox`/`Asksvin`/`Moose`/`Moose_spiritcaller`), then a dispatched background audit agent found 4 more (ItemStand, ArmorStand, Sign, Vagon) via a per-component check against `valheimtools.stream/wiki/components/<X>` — verifying "does this exact prefab carry this exact Component," not just "is this a real item name" (a real methodology correction from the maintainer mid-session).
3. **Locked the hook's keyword list** against spec.md/the ledger — found and fixed 2 real gaps (bare `owner`/`creator` needed instead of just `ownership`, since `IsOwner()`/`GetOwner()`/`GetCreator()` etc. all substring-match the shorter forms). Opened and closed [ticket 04](.scratch/ew-wiki-player-identity/issues/04-build-ownership-verification-hook.md) (task type) for the build.
4. **Built `guard-pid-ownership-ledger.cjs`** (`.agents/hooks/ew_toolkit/`) — PreToolUse hook, flags `.mdx`/`.md` edits containing the locked ownership-keyword list unless the file is the ledger/spec themselves, scans only newly-added text. Wired into both `.claude/settings.json` (Claude Code) and `ew_toolkit-cursor/.cursor/hooks.json` (Cursor, `ask`→`deny` severity translation per the dual-harness pattern). Documented in `docs/agents/hooks-vs-rules.md`. Smoke-tested 8 cases across 2 rounds, all passed.
5. **Built the companion `AGENTS.md` rule** (after `/writing-for-agents`, maintainer-confirmed text first) — new item 4 in `ew_wiki/AGENTS.md`'s "Never guess — real source first" list, pointing any ownership/`pid`/`cid` claim at the ledger before writing it. Found `ew_toolkit-cursor/ew_wiki/AGENTS.md` significantly stale (several unrelated sections too, not just this one) — resynced by copying the main worktree's file over wholesale.
6. **Rebuilt `object-ownership.mdx` into Prototype 4's shape** (`ew_wiki/src/content/docs/ewp/extended-reading/object-ownership.mdx`) using the now-locked ledger as ground truth — kept the explanatory prose sections (Claiming Player / Zone Host / Effects), closed with a no-table appendix, one full paste-ready script per object grouped by real component/prefab. Caught and fixed **3 real factual errors** that predated the ledger: ItemDrop (old page claimed it never claims itself — wrong, it does on pickup), Turret (old page claimed no watchable field — wrong, `targets` is real and watchable), TerrainModifier (old page asserted a specific proven trigger, `TCData`, that the ledger doesn't actually back up — softened to "not proven"). Added a new Character/Ragdoll section and a Feast section not present in the prototype. **All 24 YAML script blocks validated clean** against the real `ewp_validator` CLI (`ewp_validator/dist/cli.mjs`) — zero errors/warnings.
7. **Visual walkthrough in the WSL dev server**, full page top-to-bottom, dark mode — confirmed clean rendering (banner, boxes, every code block, every correction callout).
8. **PDF export for offline word-by-word review** (maintainer's request — the dev-server preview kept dropping mid-review): first export via headless Chrome (`chrome.exe --headless --print-to-pdf`) inherited the OS dark color-scheme but rendered with unreadable black-on-black text in headers/code blocks — root cause not fully diagnosed, worked around rather than fixed. Second export forced light mode via `--blink-settings=preferredColorScheme=1`, sanity-checked by toggling the live dev-server page to light mode first and screenshotting it (clean, readable) before sending. **The light-mode PDF is what the maintainer is now reviewing** — no further page edits made pending their word-by-word pass.

**Dev server flakiness note**: the WSL `ew_wiki-dev` preview server died silently more than once this session (`preview_start` reported success but nothing was actually listening; `preview_logs` showed `Another astro dev server is already running` pointing at a PID that was itself unreachable) — worked around each time by re-running `preview_start` and polling `curl` until real 200s, not yet root-caused. If this recurs, check for orphaned `astro dev` processes inside WSL (`pgrep -fa 'astro dev'`) before assuming the port is actually free.

## Untouched from the 2026-09-14 session below (still true)

Picked up where the prior session left off: [ticket 1](.scratch/ew-wiki-player-identity/issues/01-page-shape-and-placement.md) already resolved (2 pages: `Advanced Functions - Player Identity`, built/signed-off/live; `Object Ownership`, the deep-dive, still to build). This session was entirely spent building and repeatedly correcting that second page — real page at `ew_wiki/src/content/docs/ewp/extended-reading/object-ownership.mdx`.

**The pattern this whole session**: the maintainer caught a real content or format mistake, I fixed it, then the maintainer caught the *next* one underneath it. In order:
1. First draft shipped a **made-up** terrain example (invented prefab, invented fields) instead of checking real source — caught, and checking real source (`expand_prefabs_BFV_systemRestriction_LockTerrain.yaml`) revealed terrain edits actually do have a real ownership mechanism (`TerrainModifier`/`_TerrainCompiler`), contradicting what I'd written.
2. A "verified table" cited raw C# file:line numbers as if a wiki reader could open and check them — maintainer called this out as effectively unverifiable to anyone but me. Replaced every citation with a real, paste-in-your-own-game "try it yourself" EWP script per row instead.
3. Several of those "try it yourself" scripts were themselves wrong on inspection: a `Fish`/`hooked` example that would double-fire on release too (fixed: pin to value `1`); a `type: time, tick 5` example that's actually a broadcast spam/crash risk (`tick` fires many times a second, not every 5 seconds — fixed to a pull-based chat command instead); a `CookingStation` row I'd marked "no single field" despite already having found the real fields (`slotstatus0/1/2`) earlier in the same session and not using them; a whole missing FX/VFX section from the original hand-tested list, restored with real `vfx_player_hit`/`vfx_Burning` examples.
4. Page **format** itself was rejected as "exhausting" and redundant (two tables saying the same thing; abstracted "field name" cells the reader can't turn into a working script without guessing). Built and live-hosted **4 real format prototypes** side by side in the WSL dev server for the maintainer to compare — maintainer picked **Prototype 4** (keep the current page's explanatory prose sections, but close with a no-table appendix, one full script per object, grouped by component). Prototypes 1/2/3 deleted this session; **the live page itself has not yet been rebuilt into Prototype 4's shape** — that rebuild is still pending.
5. While comparing, the Tameable/ridden row turned out to cite a field (`attachJoint`) that actually belongs to a completely different object (the battering ram) — fixed to the real field, `user`, confirmed against a real prefab (`Lox`) already used in this repo's own scripts.
6. Then a **deeper mechanism** mistake: the chest example used `InUse` (open/close), maintainer said that's wrong, taking/storing items is what matters — I "fixed" it to `items` only. Checking `Container.cs` properly (specifically `RPC_RequestOpen`) showed **both were incomplete**: opening a chest transfers ownership immediately via a direct `SetOwner(uid)` call, before anything is touched — so `InUse` flipping *is* a real, valid signal for that moment, and `items` is a separate, later signal for contents actually changing. Fixed to show both, correctly labeled.

**Where the methodology argument landed (unresolved, mid-discussion as of this update)**: the maintainer is (rightly) no longer willing to accept sampled/ad-hoc source checks after this many rounds of correction. Mid-session pivot to build a **standing "pid ownership code findings" ledger** — a new file (location decided: sibling to `ew_wiki/docs/guide-source/3-advanced-guide/EWP_pid_behavior.md`) that records every real ownership-changing code path, to be consulted *and extended* before any future ownership-topic edit, enforced by a broadened, blocking hook (not the narrower one first proposed).

Real progress on this mid-discussion: checked `ZDO.cs` itself (the class that owns the concept) rather than sampling call sites — found the **provably complete** mechanism: exactly two methods, `SetOwner(uid)`/`SetOwnerInternal(uid)`, are the *only* way anything in the entire codebase can change a ZDO's owner (the field is private). `ClaimOwnership()` on `ZNetView` is just a named wrapper (`m_zdo.SetOwner(ZDOMan.GetSessionID())`), not a separate mechanism. This is a genuinely strong finding — but the maintainer explicitly does **not** want today's earlier findings (Container, Fireplace, Sign, etc.) backfilled into the new ledger as-is, calling them "loopholes everywhere" — wants a full, fresh, exhaustive sweep instead (every call site of `SetOwner`/`ClaimOwnership` across the *entire* decompile, count reported, every hit accounted for), likely as a background `/research` subagent pass.

**Not yet answered as of this update** (asked via `AskUserQuestion`, mid-turn when the session ended): whether to dispatch that full sweep now as a background research agent, and whether the proposed broadened hook keyword list (`<pid>`, `<cid>`, `ClaimOwnership`, `SetOwner`, `SetOwnerInternal`, `ZDOVars.s_owner`, `ZDOVars.s_creator`, `long_creator`, `long_owner`, `ReleaseZDOS`, "zone host"/"ownership") is complete or needs more terms the maintainer already knows about. **Nothing on the ledger/hook has been written to disk yet** — this was explicitly "plan and review before building anything."

## Uncommitted this session (2026-09-16, later)

`ew_wiki/src/content/docs/ewp/extended-reading/object-ownership.mdx` — ticket 13 fully done (Personal FX + Projectile, signed off), PLUS today's unreviewed chunk-5 restructure on top: Character/Ragdoll and OfferingBowl sections deleted outright, Tameable section deleted and folded into Sadle as an alias Tip, component table at the top updated to match. **None of the chunk-5 restructure has been shown to the maintainer live yet** — that's the very next step, not a formality. `.scratch/ew-wiki-player-identity/map.md` (ticket 13's Decisions-so-far entry added, ticket 11's frontier-list line needs another pass once chunk 5 actually closes), `issues/13-fx-object-rule.md` (closed, Status + Answer written), `issues/11-object-chunk-5.md` (still open, still describes the OLD 3-object shape — stale, not yet rewritten to match what's actually on the page now). `draft: true` and `astro.config.mjs` still deliberately untouched. No commits made this session; nothing has been asked to be committed yet.

## Next step

1. **Restart the WSL dev server and show the maintainer the live-rendered page** — the chunk-5 restructure (Character/Ragdoll gone, OfferingBowl gone, Tameable folded into Sadle) has never been seen rendered. Do this before anything else; don't touch ticket 11's file or the map until this is confirmed good.
2. **Once signed off**, rewrite [ticket 11](.scratch/ew-wiki-player-identity/issues/11-object-chunk-5.md)'s Answer to match what actually happened (2 objects removed, not resolved — probably belongs partly in the map's Out of scope section per wayfinder's own rule for a ticket that turns out to sit past its original question, not just pasted into Decisions-so-far as if it were a normal close), then close it and update the map.
3. Move to [ticket 12](.scratch/ew-wiki-player-identity/issues/12-object-chunk-6.md) (Smelter/Beehive/Piece-WearNTear/Feast) — the last object-appendix chunk once ticket 11 is settled.
4. Only once **every** chunk ticket (7-13, now effectively 7-12 given 13's own scope) is signed off: remove `draft: true` and wire the page into `astro.config.mjs`'s Extended Reading sidebar, **together, in one pass** (standing rule, `feedback_wayfinder_prototype_before_final_content.md`).
5. The `<long_creator>`/`<long_owner>` placement question is still genuinely undecided — no ticket currently owns it.
6. Consider writing a standing note in `ew_wiki/AGENTS.md` about the WSL dev-server restart-not-reload requirement, and the `MSYS_NO_PATHCONV=1` / foreground-plus-`run_in_background` restart pattern this session leaned on repeatedly — still proposed, not yet acted on.
7. Ask before committing this session's changes — nothing has been committed, and the pile spans content, tickets, and the map.
8. If the dev-server flakiness recurs, check for an orphaned `astro dev` process inside WSL before re-running blind — has happened on multiple sessions. This session specifically: a Windows-side file edit never reaches the WSL dev server without a full restart (kill + relaunch), a reload alone is not enough.

Everything below this line is prior-session history, unchanged.

---

Prior update: 2026-09-14 (earlier same day). A `/wayfinder` session on [Ew Wiki Real Build](.scratch/ew-wiki-real-build/map.md): resolved [ticket 26](.scratch/ew-wiki-real-build/issues/26-richtext-tags-section.md) (RichText tags), then reopened [ticket 27](.scratch/ew-wiki-real-build/issues/27-player-identity-functions-research.md) after its original research turned out wrong — a background research agent is now redoing it (see below), not yet returned as of this update.

## This session, part 2: ticket 27 reopened, ticket 28 scrapped and re-blocked

Picked up [ticket 28](.scratch/ew-wiki-real-build/issues/28-player-identity-page-shape.md) (player-identity page) after ticket 26. Built a first "Object Ownership" page as a `draft: true` prototype (Starlight's own field — excluded from the sidebar/production build, still viewable in the dev server — see the new memory `feedback_wayfinder_prototype_before_final_content` for why that flow exists), got it reviewed live in the running dev server per the maintainer's explicit "prototype first" instruction.

**The page's central claim turned out wrong**, caught by the maintainer: it said "no field anywhere in EWP persistently records who placed an object." Real, persistent field: `Piece.creator` (`m_creator`/`SetCreator`/`GetCreator` in `Piece.cs`, backed by ZDO key `"creator"` in `ZDOVars.cs`, in `valheim-modding/decompiled/`) — never checked because ticket 27's original research only read EWP's own mod source (`PeerManager.cs`/`ObjectFunctions.cs`), never the base-game decompile sitting in this same machine, and never consulted the maintainer's own `ew_wiki/docs/guide-source/3-advanced-guide/EWP_pid_behavior.md` (a hand-tested empirical map of per-component ownership behavior) or `.../4-script-examples/99-bfv-examples/` (22 real scripts using `pid`/`cid`, revealing a `<pid>/<cid>`-as-EWP-key-namespace technique — `<save_<pid>/team_1>` — used across ~10 real production systems, among others).

Reopened [ticket 27](.scratch/ew-wiki-real-build/issues/27-player-identity-functions-research.md) with a documented "Reopened" section (same convention ticket 03 used); re-blocked [ticket 28](.scratch/ew-wiki-real-build/issues/28-player-identity-page-shape.md) pending the redo. Deleted the scrapped `object-ownership.mdx` (was untracked, `draft: true`, never merged into the sidebar — safe to remove outright rather than leave a wrong-premise file lying around). Updated the map's Decisions-so-far entry for ticket 27 to reflect the reopening rather than the old (wrong) resolution summary.

**The background research agent returned** (per wayfinder's own `research` ticket-type design — AFK, resolved by a `/research` subagent): [research_reports/valheim-ewp-player-identity-ownership-2026-09-14.md](../research_reports/valheim-ewp-player-identity-ownership-2026-09-14.md), read all 22 `pid`/`cid` scripts in full plus a whole-decompile grep for `ClaimOwnership()` (14 files, not the 10 spot-checked earlier) and persistent-field patterns. Found a **second** real persistent base-game field beyond `Piece.creator` — `Bed.s_owner`, a same-name-but-unrelated slot to the ZDO's own transient network-owner. Every row of the maintainer's `EWP_pid_behavior.md` now traces to source or is explicitly flagged unconfirmable (no guessing). Ticket 27 re-resolved for real; ticket 28 stays re-blocked — the sizing verdict flipped to "genuinely 6 sections, needs its own map," proposed on the map's Not yet specified but not yet chart-approved by the maintainer.

**New standing artifacts from this session**: `feedback_wayfinder_prototype_before_final_content.md` and `project_ew_wiki_wsl_only_toolchain.md` (both new memory files — see `MEMORY.md` at `C:\Users\Ultimate\.claude\projects\C--Users-Ultimate-Claude-ew-toolkit\memory\`), plus a new hook `guard-new-wiki-page-draft.cjs` (wired into both `settings.json` files, `docs/agents/hooks-vs-rules.md` updated) that asks before a brand-new `ew_wiki` page gets written without `draft: true`.

## This session, part 1: ticket 26 resolved (RichText tags)

Claimed and resolved [ticket 26](.scratch/ew-wiki-real-build/issues/26-richtext-tags-section.md), the first frontier ticket on the [Ew Wiki Real Build map](.scratch/ew-wiki-real-build/map.md) after the charting pass below opened it. Checked the live wiki (not just guide-source) for real RichText call sites first — found only two: `basic-rpcs.mdx`'s `ShowMessage` example and one `Beehive.m_name` value on `bee-ecosystem.mdx`. Built one shared snippet, `ew_wiki/src/components/snippets/richtext-tags-note.mdx` (small `color`/hex-shorthand/`br`/`size` table, cited from the validator's earlier RichText research in `.scratch/validator-round5/research/02-valheim-richtext-tag-source-audit.md`), taught in full on `basic-rpcs.mdx` under a new "Coloring and formatting a message" subsection, with just a one-line pointer Aside on `bee-ecosystem.mdx`. Build verified clean via WSL, 33 pages (same count — no new route). Updated the ticket's Answer and the map's Decisions-so-far.

3 open tickets remain on that map's frontier, all unblocked (their blocking research tickets are already resolved): [ticket 28](.scratch/ew-wiki-real-build/issues/28-player-identity-page-shape.md) (player-identity page), [ticket 30](.scratch/ew-wiki-real-build/issues/30-iter-functions-page.md) (`iter`/`iter2` page), [ticket 32](.scratch/ew-wiki-real-build/issues/32-locations-page-decision.md) (locations decide+write). Per wayfinder's one-ticket-per-session rule, none of these three were touched this session.

**Newly uncommitted on top of the charting pass below**: `ew_wiki/src/content/docs/ewp/concepts/basic-rpcs.mdx`, `ew_wiki/src/content/docs/ewp/examples/bee-ecosystem.mdx` (both modified), `ew_wiki/src/components/snippets/richtext-tags-note.mdx` (new), plus `.scratch/ew-wiki-real-build/issues/26-richtext-tags-section.md` and `.scratch/ew-wiki-real-build/map.md` (both already untracked/modified from the charting pass, now further updated). Same "ask before committing" standing rule applies — not committed yet.

## Where things stand (charting pass, 2026-09-14, prior to ticket 26)

**This session (2 parts)**: (1) a read-only `/wayfinder` audit over all `ew_wiki`-related maps plus the 2 toolkit-wide maps that name `ew_wiki`/Tool #2 as a trigger, looking for stale fog, dead OOS items, and forgotten "revisit once X" triggers; (2) a follow-up charting pass acting on the maintainer's review of those findings — this part **did** touch the maps (fixed a confusing map summary, resolved 2 quick decisions, opened 15 new tickets across 3 maps). No wiki content or code was written — every new ticket is open, awaiting its own session.

**Part 1 findings, now acted on in Part 2:**
- Hub map's "does the Tool-adding mechanism hold up a second time" and Changelog Automation's "how does Tool #2 get attributed in releases" — both fired (Tool #2/`ew_wiki` shipped), both now real open tickets: [Hub ticket 23](.scratch/ew_toolkit/issues/23-tool-adding-mechanism-second-pass-review.md), [Changelog ticket 06](.scratch/changelog-automation/issues/06-tool-2-attribution-in-releases.md).
- Fixed the confusing "5 live-page bugs" summary in the [Ew Wiki Real Build map](.scratch/ew-wiki-real-build/map.md)'s Round 2 section — it now accounts for all 5 explicitly.
- Checked the 3 deferred Phase 2 guides (Advanced Poke, Maths, Ship Modification) against the live site: their core content is **already superseded** by later tickets under different page names. Only real survivor: `<pname>` (player-identity) is used decoratively in 2 of the 3 but never explained anywhere — see below.
- Reference section (exhaustive schema-generated docs) — resolved **don't build it**, link to Jere's own `docs/functions.md`/`docs/scripting.md` instead: [ticket 24](.scratch/ew-wiki-real-build/issues/24-reference-section-decision.md).
- Troubleshooting section — resolved **don't build it**, the validator already does this live and the bad-cop pattern covers what it doesn't: [ticket 25](.scratch/ew-wiki-real-build/issues/25-troubleshooting-section-decision.md).
- Wiki Tool Prototype map's "larger-scale IA validation" fog — closed with real evidence (ticket 23's 7.5-round friction history + `ew_wiki/docs/widget-build-notes.md`) rather than a new synthetic validation ticket.
- Content-quality-pass map had a "recheck the 18-item sweep report" reminder that appeared **repeatedly across sessions** (map Notes, map fog, `HANDOFF.md`'s own Next step, more than once) without ever becoming a ticket — now closed the loop: [ticket 14](.scratch/ew-wiki-content-quality-pass/issues/14-recheck-sweep-report-claims.md) (the recheck itself) and [ticket 15](.scratch/ew-wiki-content-quality-pass/issues/15-standing-bad-cop-rule-decision.md) (the standing bad-cop-rule decision that was sitting on the same kind of un-ticketed hold).

**"Lost topics" sweep (same shape as player-identity — deeply researched for the validator, never carried to the wiki), maintainer-reviewed 2026-09-14:**
- `playerEvents`/`bannedPlayerEvents` — ruled **out of scope**, rarely used (maintainer's call).
- RichText tags (`<color=...>`, `<size=...>`) — small section, not a solo page: [ticket 26](.scratch/ew-wiki-real-build/issues/26-richtext-tags-section.md).
- Player-identity (`pid`/`cid`, object-ownership focus) — solo page, needs deep game-code research first: [ticket 27](.scratch/ew-wiki-real-build/issues/27-player-identity-functions-research.md) (research, blocks) → [ticket 28](.scratch/ew-wiki-real-build/issues/28-player-identity-page-shape.md) (page shape/write).
- `iter`/`iter2` — complex advanced-math functions, needs source-verified research first: [ticket 29](.scratch/ew-wiki-real-build/issues/29-iter-functions-research.md) (research, blocks) → [ticket 30](.scratch/ew-wiki-real-build/issues/30-iter-functions-page.md) (page).
- `locations`/`bannedLocations`/`exteriorRadius` — sizing genuinely unclear (page vs. section vs. skip), research first: [ticket 31](.scratch/ew-wiki-real-build/issues/31-locations-research.md) (research, blocks) → [ticket 32](.scratch/ew-wiki-real-build/issues/32-locations-page-decision.md) (decide + write).
- Ran one more full sweep across remaining top-level EWP action/filter keywords (`cancel:`, `attach:`, `connect:`, `objectsLimit`, `filterLimit`, `excludePrefab`, `separate`, `removeDelay`) — all already covered somewhere on the live wiki. No further undocumented major topics found.

**Still open, needs the maintainer's answer**: whether 2 of this session's decisions should graduate into standing rules beyond their own ticket — (a) [ticket 24](.scratch/ew-wiki-real-build/issues/24-reference-section-decision.md)'s "link to upstream docs instead of rebuilding a reference" as a general `ew_wiki/AGENTS.md` content rule, and (b) [ticket 25](.scratch/ew-wiki-real-build/issues/25-troubleshooting-section-decision.md)'s "wiki teaches, validator catches" division of labor as a `CONTEXT.md`/`AGENTS.md` note about how `ew_wiki` and `ewp_validator` divide responsibility — asked, not yet answered as of this update.

## Prior sessions (unchanged from before this session)

**This session**: maintainer reported Valheim and EWP both got updates and asked for fixes. Checked EWP's real GitHub source (`JereKuusela/valheim-expand_world_prefabs`) field-by-field against `ewp_validator`'s hand-maintained schema (`ewp_validator/schema/generate.mjs` — a deliberately static schema, doesn't auto-track new EWP fields, see that file's own header comment). Didn't need to touch Valheim's own game code — EWP already absorbed the game-update changes internally (its CHANGELOG says v1.60 "Fixes for the new game update"); the validator only needs to track EWP's own script surface. Found one real gap: `log:`, a new top-level action (`PrefabData.cs:50`) that writes a line to `ewp_log.txt` instead of a chat command — was missing from the schema, so any script using it got a false "unknown field" error. Everything else new in v1.59/v1.60 (`random:` on pokes, alt-biome support on `biomes:`/`bannedBiomes:`) was already correctly handled.

Fixed in two commits, pushed to `origin/main`:
- `84825e4` — added `log: str` to the schema's Actions section, regenerated `schema.generated.json` (confirms EWP 1.60.0), slotted `log` into the root `AGENTS.md` script field-order convention (right after `command`/`commands`, maintainer's call), synced that same line to the `ew_toolkit-cursor` worktree's copy of `AGENTS.md` (left **uncommitted** there deliberately — that branch belongs to the Cursor agent, not this session, per the one-agent-at-a-time rule).
- `4bc28f3` — caught this file itself sitting modified-but-uncommitted from the prior session (documented the tickets 06-13 push) and committed it before starting today's work.

Per maintainer's explicit call, did **not** add wiki teaching content for `log:` — validator fix only for now; a `log:` teaching page is a separate future decision, not chartered anywhere yet.

**Known gap this session hit**: couldn't run `ewp_validator`'s actual test suite locally — `vitest` fails to start on this Windows checkout (`Cannot find native binding` / `@rolldown/binding-wasm32-wasi`), the same class of issue as the existing "this repo's lock must be built on Linux" note. Hand-verified the `log:` schema fix instead with a direct Ajv compile+validate check. **Next session on a Windows checkout should expect the same vitest failure** — real test suite needs WSL/Linux, same as the wiki's dev server.

**CI caught something local checks missed**: the push above triggered a real CI failure (run `34701323651`) — 2 tests in `rpcValidation.test.ts` failed because upstream `RPCs.md` now documents `RPC_SetVisualItem`'s 2nd param as type `hash`, not `string` (confirmed live against the doc and against `RpcInfo.cs`'s `Types` set, which treats `hash`/`string` as genuinely different — `hash` applies `GetStableHashCode()`). This is exactly the kind of EWP-update drift the maintainer flagged this session; CI's live `docs/RPCs.md` fetch (via `schema/generate.mjs`) surfaced it since the hardcoded test expectations hadn't been updated. Fixed both assertions to expect `hash`; pushed as a follow-up commit. **Still unverified locally** (same vitest breakage) — check CI on the next push to confirm green.

## Prior sessions

Committed and pushed today, three commits total on top of origin/main (12 older ones plus this session's): (1)+(2) the nav-bar/favicon/header-TOC backlog and the content-quality-pass's first pass (3 wording fixes, new teaching content, 2 bad-cop examples, the 18-item sweep) — both already described lower in this file from the prior session; (3) **this session**: resolved the map's remaining 8 tickets (06-13), adding WRONG/CORRECT bad-cop pairs across 15 `ew_wiki` concept/example pages. Each claim was cross-checked against the mod's real C# source after an earlier prototype pass had wrongly claimed `<par2>` "fails to resolve" (it doesn't — `Functions.cs` has it as a real hardcoded shortcut); that pair was dropped instead of shipped. Also retired a planned bad-cop pair on `basic-rng.mdx` (weight-sum-under-1 isn't actually a mistake — logged as Out of scope on the map instead). Pushed to `origin/main` (`fe4e468`); GitHub Pages should pick it up automatically.

**The content-quality-pass map's destination is now fully met** — all 12 of its tickets are closed. One fog item remains un-ticketed on the map itself (not a task, just noted): re-checking the original 18-item sweep report for other unverified "definition"-style claims, same class of error as the `<par2>` and comma/semicolon mistakes.

Prior narrative on the backlog commit (ticket 23 round 7.5 wrap-up):

Ticket 23 (`ew_wiki` header/TOC layout) has gone through 7.5 rounds today — see the ticket file for the full detail on each. Round 6 checked mobile width (375px) and fixed 2 real overlap bugs there. Round 7: the maintainer caught the theme toggle rendering as a colorful emoji on ew_wiki vs. a plain symbol on landing/validator — root cause was `shared/theme.css`'s `.theme-toggle` rule inheriting `font-family` from whichever host page it's dropped into; fixed by pinning `font-family` explicitly on `.site-nav` as a whole. Round 7.5: checking landing/validator as asked (not just re-trusting the round-7 close-out) found that fix hadn't actually reached the button on landing — `<button>` elements never inherit `font-family` from ancestors by default in any browser, so the pin on `.site-nav` (a `<div>`) never reached its child button; `ew_wiki` and `ewp_validator` only "verified fine" in round 7 because each happens to carry its own separate button-font reset that landing doesn't have. Fixed by restoring an explicit `font-family: inherit` on `.theme-toggle` itself, on top of the bar-level pin.

**Ticket 23 has no more known open issues** as of round 7.5 — see the ticket file for the full verification detail on every round, including the live re-check across all 3 pages (identical 53px nav height, identical computed button font, zero console errors on landing/validator/wiki).

**New hub-level hook**: `guard-shared-verify-all-tools.cjs` (`.agents/hooks/ew_toolkit/`, wired in both `settings.json` files as `PostToolUse` on `Edit|Write`). Fires whenever a `shared/` file changes, reminding to verify computed styles live on all 3 Tools before calling the change done — the trigger (a file path under `shared/`) is mechanical, so per `docs/agents/hooks-vs-rules.md`'s test it earned a hook, not just a memory. Smoke-tested (fires on a `shared/` path in either worktree, silent on unrelated paths) before wiring. Also added a one-sentence cross-reference in `AGENTS.md`'s "Shared visual identity" mechanism pointing at it, and logged the hook's own entry in `docs/agents/hooks-vs-rules.md`.

The `ew_wiki` interactive-widget phase (tickets 17-22 on the [Ew Wiki Real Build map](.scratch/ew-wiki-real-build/map.md)) is otherwise closed. Lessons from that phase live in [ew_wiki/docs/widget-build-notes.md](ew_wiki/docs/widget-build-notes.md), read before starting any new widget.

`ai-workflow-audit` (root-level map, `ai-workflow-audit/map.md`) round 2: this HANDOFF used to say ticket 17 was partway through its own checklist — checked while writing this update, and the map itself says otherwise: "ticket 17 ... is also resolved. Nothing currently fog." Correcting the stale claim rather than repeating it. No open frontier on that map as of this check.

## Uncommitted / in progress

**This session's `/wayfinder` work is uncommitted** (`git status` confirmed): 5 modified map files (`ew-wiki-real-build/map.md`, `ew-wiki-content-quality-pass/map.md`, `wiki-tool-prototype/map.md`, `ew_toolkit/hub-map.md`, `changelog-automation/map.md`) plus this `HANDOFF.md`, and 10 new untracked ticket files (Hub ticket 23; Changelog ticket 06; content-quality-pass tickets 14-15; real-build tickets 24-32). Per the map's own standing rule ("nothing here gets committed without asking the maintainer first"), **not committed yet — ask before committing.**

Everything before this session (the `log:` schema fix, the nav-bar/favicon/header-TOC backlog, the content-quality-pass's first 13 tickets) was already committed and pushed in prior sessions, `main` matched `origin/main` before this session started. `.agents/hooks/ew_toolkit/guard-shared-verify-all-tools.cjs` and `guard-script-block-header-comment.cjs` still live outside this git repo entirely (the project root isn't a git repo), so they're not in `git log` — no action needed, just noting it so a future session doesn't go looking for them there.

- Commit 1 (backlog): everything from resolving [ticket 21](.scratch/ew_toolkit/issues/21-unify-hub-tool-nav-bar.md), [ticket 22](.scratch/ew_toolkit/issues/22-unify-favicon.md), and [ticket 23](.scratch/ew-wiki-real-build/issues/23-header-toc-layout-rework.md) — nav bar unification, favicon dedup, header/TOC layout rework (all 6+ rounds). Includes `shared/navBar.ts`, `shared/theme.ts`, `shared/favicon.png`, `scripts/sync-favicon.mjs`, `docs/agents/hub-tool-nav.md`, `src/nav.ts`, `src/style.css`, `package.json`/`ewp_validator/package.json`/`ew_wiki/package.json`, `ewp_validator/src/main.ts`+`style.css`, `ew_wiki/astro.config.mjs`, `ew_wiki/src/components/{Header,CollapsibleToc,PageTitle,PageSidebar}.astro`, `ew_wiki/src/styles/theme.css`, `shared/theme.css`, `shared/icons.ts`, `ew_wiki/docs/widget-build-notes.md`, favicon swap, both ticket files and their maps. Note: ticket 23 and tickets 21/22 turned out to have edited the same lines in `.claude/settings.json` and root `AGENTS.md` in alternation (interleaved sessions) — not worth a risky manual line-level split, so they're one combined commit rather than three.
- Commit 2: the content-quality-pass's first pass (tickets 01-05) — see prior handoff narrative above.
- Commit 3 (`fe4e468`, this session): content-quality-pass tickets 06-13, all 8 bad-cop tickets — see "Where things stand" above.

All three pushed to `origin/main` this session (13 commits went out together, since 1 and 2 were sitting locally, committed but unpushed, from before).

## Next step

**Get the maintainer's go-ahead to chart a new wayfinder map** for player-identity/object-ownership — [ticket 27](.scratch/ew-wiki-real-build/issues/27-player-identity-functions-research.md) is fully re-resolved now (see "This session, part 2"), and its sizing verdict says this needs 6 sections, not one page. Proposed on the [Ew Wiki Real Build map](.scratch/ew-wiki-real-build/map.md)'s Not yet specified, asked but not yet confirmed as of this update. [Ticket 28](.scratch/ew-wiki-real-build/issues/28-player-identity-page-shape.md) stays re-blocked/parked until that's decided — don't write it as a single page.

**Or pick a different frontier ticket instead** on [Ew Wiki Real Build](.scratch/ew-wiki-real-build/map.md) while that agent runs: [ticket 30](.scratch/ew-wiki-real-build/issues/30-iter-functions-page.md) or [ticket 32](.scratch/ew-wiki-real-build/issues/32-locations-page-decision.md) — both open, unblocked, unclaimed. One per session per wayfinder's rule.

**Standing reminder for any content-writing ticket on this map**: build it as a `draft: true` prototype first (viewable in the dev server, excluded from the sidebar/production build), get explicit maintainer signoff on the actual rendered page, *then* remove `draft: true` and wire it into `astro.config.mjs`'s sidebar together, in one pass. See the memory `feedback_wayfinder_prototype_before_final_content` for the full flow and why — caught twice in one session before it was written down.

**Get the maintainer's answer on the 2 pending standing-rule questions** raised at the end of "Where things stand" (link-out-instead-of-rebuilding as an `ew_wiki/AGENTS.md` rule; wiki/validator division-of-labor as a `CONTEXT.md`/`AGENTS.md` note) — asked, not yet answered.

**Ask before committing this session's `/wayfinder` map/ticket edits** — see Uncommitted above; the map's own standing rule requires maintainer sign-off first.

**14 open tickets now exist across 3 maps**, ready for future sessions (never resolve more than one per wayfinder's own rule): Hub ticket 23, Changelog ticket 06, content-quality-pass tickets 14-15, real-build tickets 26-32 (tickets 24-25 already resolved this session). [Ticket 27](.scratch/ew-wiki-real-build/issues/27-player-identity-functions-research.md) and [ticket 31](.scratch/ew-wiki-real-build/issues/31-locations-research.md) are `research` type — good candidates for an AFK `/research` subagent pass whenever picked up, per wayfinder's charting step.

**Check CI on the next push** (or `gh run list` now) — confirm the `rpcValidation.test.ts` fix actually turned the build green; it was pushed but not yet re-verified as of the last code session (2026-09-12). If it's still red, don't assume it's the same `hash`/`string` issue — read the new log first.

`ai-workflow-audit` has no open frontier (see the correction above — don't repeat the stale "ticket 17 partway" claim this file used to carry). One older loose thread, still not chartered: whether `ewp_validator`'s hand-maintained schema (`schema/generate.mjs`) should get a periodic "diff against EWP's real field list" check, rather than waiting for a maintainer to notice drift like `log:` by chance.

## Don't touch

(none currently — the nav-bar/layout-bugs park lifted 2026-09-11, see Next step)

## Dual-agent note

If a Cursor session picks this up instead of Claude Code: pull first. Standing-rule file sync (`AGENTS.md`/`CLAUDE.md`/`CONTEXT.md`/`docs/agents/`) is hook-enforced now (`guard-standing-rules-sync.cjs`, blocks session end on drift) — see `docs/agents/hooks-vs-rules.md`.
