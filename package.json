{
  "name": "ew-toolkit-landing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-favicon.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-favicon.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "build:hub": "node scripts/build-hub.mjs",
    "cut-release": "node scripts/cut-release.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
